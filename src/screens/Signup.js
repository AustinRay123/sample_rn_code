import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { Signup as SignupComp } from '../features'
const Signup = (props) => {
  return (
   <SignupComp props={props}/>
  )
}

export default Signup
