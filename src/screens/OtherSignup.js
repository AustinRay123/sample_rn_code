import { View, Text } from 'react-native'
import React from 'react'
import { OtherSignup as OtherSignupCommp } from '../features'

const OtherSignup = (props) => {
  return (
    <OtherSignupCommp props={props}/>
  )
}

export default OtherSignup